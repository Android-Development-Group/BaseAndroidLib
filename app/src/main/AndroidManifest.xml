<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobisoft.mbstest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Base.dingyingapp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:name,android:theme,android:label,android:roundIcon,android:icon,android:allowBackup">
        <activity
            android:name=".splashScreen.SplashActivity"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".index.MainActivity" />
        <activity android:name=".index.IndexActivity" />
        <activity android:name=".index.WebActivity" />
        <activity android:name=".index.TestDyActivity"></activity>

        <meta-data android:value="5c04ca4bf1f5569d41000223" android:name="UMENG_APPKEY"/>
        <meta-data android:value="mobisoft" android:name="UMENG_CHANNEL"/>
    </application>

</manifest>